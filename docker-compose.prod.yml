services:
  # -----------------------------------------------------------------------
  # 배포(EC2) 오버라이드
  # - 기본: 로컬 8000:8000
  # - 배포:  80:8000
  # -----------------------------------------------------------------------
  api:
    ports:
      - "80:8000"
